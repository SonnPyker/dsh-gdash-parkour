// dsh-gdash-parkour host stub - không cần logic host, chỉ để bundle hợp lệ
// DSH sẽ load client half từ dsh.client (client/client.js)
export function apply() {
  // no-op host, toàn bộ game chạy ở client
}
