// dsh-gdash-parkour/src/client.ts - source (tham khảo, build ra lib/client.js)
// Build: copy nội dung này vào lib/client.js hoặc dùng tsdown

import type { ClientContext } from "@deepseek-ai/dsh-client-runtime/client";
// Xem lib/client.js để có bản chạy thực tế (plain JS, không cần build)
export const inject = ["timer"];
export function apply(ctx: ClientContext) {
  // implementation in lib/client.js
}
